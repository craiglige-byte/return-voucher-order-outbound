> **文档名称**: PRD — 下游返货券订单出库
> **所属项目**: 供应链进销存系统
> **用途**: 下游返货券订单出库模块产品需求文档，供研发评审与实现使用。
> **Owner**: Black
> **版本**: v1.0
> **最后更新**: 2026-08-17
> **上游基线**: 无（原型迭代项目，基于现有 Vite 原型代码与需求讨论）
> **内容概述**: §一 文档概要。§二 背景与目标。§三 竞品分析。§四 主逻辑（含规模要求）。§五 方案详情（顶部统计区 / 订单列表 / 待出库商品统计弹窗 / 订单详情弹窗）。§六 迭代计划。§七 运营计划。§八 沟通记录。§九 参考资料。

---

## 目录

1. 文档概要
2. 背景及目标
3. 竞品分析
4. 主逻辑
5. 方案详情
6. 迭代计划
7. 运营计划
8. 沟通记录
9. 参考资料

---

# 一、文档概要

## 1.1 版本记录

| 版本 | 更新时间 | 更新人 | 更新内容 |
|------|---------|-------|---------|
| v1.0 | 2026-08-17 | Black | 初版落地：顶部统计区两段分栏、订单列表、待出库商品统计弹窗、订单详情弹窗 |

## 1.2 需求范围 / 功能范围概述

| 项目 | 内容 |
|------|------|
| 涉及系统 | 供应链进销存系统 > 进销存 > 下游返货券订单出库 |
| 需求点 | 1. 顶部统计区按「未生成返货订单 / 返货券订单」两段分栏展示三个层级指标；2. 订单列表按「待出库 / 已出库」tab 区分，支持筛选、详情查看、单笔与批量确认出库、导出；3. 待出库商品统计弹窗按品项汇总待出库瓶数；4. 订单详情弹窗展示商户、配送、签收、品项明细 |

## 1.3 名词解释

| 术语 | 定义 |
|------|------|
| 返货券 | 品牌方发放的、可兑换商品的凭证。消费者购买饮料中奖后获得返货券。 |
| 乐享券 | 经销商确认出库后自动发放给下游的权益券，出库完成即发放。 |
| 码核销 | 消费者中奖后由门店老板扫码，记录返货券核销行为，产生「码」层级的核销记录。 |
| 经销商待从下游返货 | 消费者已扫码核销、经销商尚未从下游门店取码生成返货订单的返货券总量（码层级，未成单）。 |
| 待出库（未发放乐享券） | 经销商已取码生成返货券订单、但尚未确认出库的返货券总量（订单层级）。 |
| 已出库（已发放乐享券） | 已确认出库并自动发放乐享券的返货券总量（订单层级）。 |
| 签收状态 | 订单在物流环节的签收进度，枚举：已签收 / 待签收 / 运输中 / 已拒收。 |

---

# 二、背景及目标

## 2.1 项目资料

| 项目 | 内容 |
|------|------|
| 所属系统 | 供应链进销存系统（JBP Pro 相关生态） |
| 模块名 | 下游返货券订单出库 |
| 原型路径 | `C:\Users\YQSL\返货券订单出库` |
| 技术栈 | Vite 6 + React 19 + TypeScript + Tailwind CSS 4 |

## 2.2 背景与综述

**痛点：**

1. **层级混杂**：返货券从「消费者核销」到「经销商出库」是一个跨两个层级（码 / 订单）的流水线，但原页面用 4 张平铺指标卡（待返货 / 已核销 / 已发放 / 待发放）并列展示，用户无法分辨哪些属于「码」层级、哪些属于「订单」层级。
2. **术语混淆**：「已核销」卡实为「待出库 + 已出库」的订单合计，与业务语境中的「核销」含义冲突，造成理解偏差。
3. **语义偏差**：「待返货」的说明文案（经销商未与下游批发商核销）与真实业务（消费者已核销、经销商尚未取码成单）不符。

**解决方案：**

将顶部统计区重构为「两段分组分栏」——左段「未生成返货订单」对应码层级（经销商待从下游返货），右段「返货券订单」对应订单层级（待出库 / 已出库），并用虚线框 / 实线框区分两个层级。同步统一命名与文案。

**效果：**

| 痛点 | 解决方案 | 效果 |
|------|---------|------|
| 层级混杂 | 两段分栏 + 虚/实线框区分 | 一眼区分码层级与订单层级 |
| 术语混淆 | 删除「已核销」卡，统一命名 | 指标无歧义 |
| 语义偏差 | 修正待返货文案 | 文案与业务一致 |

## 2.3 项目计划

（待人工维护）

---

# 三、竞品分析

（待人工调研，Agent 不代替生成竞品内容。）

---

# 四、主逻辑

## 4.1 核心场景矩阵

| 用户角色 | 核心场景 | 操作 | 触发方式 | 核心动作 |
|---------|---------|------|---------|---------|
| 经销商（出库人员） | 查看待出库订单并确认出库 | 切到「待出库」tab → 确认出库（单笔/批量） | 手动 | 状态由「待出库」流转为「已出库」，发放乐享券 |
| 经销商（出库人员） | 查看按品项的待出库汇总 | 点击「待出库」卡片 | 手动 | 打开「待出库商品统计」弹窗 |
| 经销商（出库人员） | 查看订单明细 | 点击「查看详情」 | 手动 | 打开「出库单详情」弹窗 |
| 经销商（出库人员） | 导出订单数据 | 点击「导出」 | 手动 | 下载当前筛选结果 CSV |

## 4.2 端到端流程概述

```
消费者购买饮料中奖
  → 找门店老板扫码核销（产生「码」核销记录）
      → 经销商从下游门店取码
          → 系统生成「返货券订单」（订单初始状态 = 待出库）
              → 经销商在本页面确认出库
                  → 订单状态流转为「已出库」+ 自动发放乐享券
```

三层级对应关系：

```
[码层级]  经销商待从下游返货 ──取码成单──▶ [订单层级] 待出库 ──确认出库──▶ [订单层级] 已出库
   （虚线框）                                 （实线框）                  （实线框）
   不在订单列表                                  在订单列表                  在订单列表
```

## 4.3 规模要求

| 指标 | 预估值 | 说明 | 来源 |
|------|--------|------|------|
| 数据量级 | 待补充 | 当前为原型 mock 数据，真实量级待接入后评估 | 待补充 |
| 页面响应时间 | ≤ 3s（常规操作） | 前端原型本地数据即时响应 | 非功能基线 |
| 数据导出限制 | 单次 ≤ 5000 条 | 超限提示分批导出 | 非功能基线 |

---

# 五、方案详情

## 5.1 顶部统计区（两段分栏）

### 5.1.1 页面原型

```
+-----------------------------------------------------------------------------------+
| 下游返货券订单出库                                            [每小时拉取一次]      |
| 支持按商户、单号、签收状态和时间快速筛选，统一处理待出库与已出库单据。              |
+-----------------------------------------------------------------------------------+
| 未生成返货订单                        |  返货券订单                                |
| ┌───────────────────────────┐       |  ┌───────────────────┬──────────────────┐ |
| │ 经销商待从下游返货      ⓘ  │       |  │ 待出库（未发放乐享券）ⓘ│ 已出库（已发放乐享券）ⓘ│ |
| │ 1,250  瓶                │       |  │ 5,542  瓶          │ 1,062  瓶        │ |
| │ 消费者已核销，经销商尚未   │       |  │ 按品项汇总待出库数量 ▸│ 已自动发放乐享券   │ |
| │ 从下游取码生成返货订单     │       |  └───────────────────┴──────────────────┘ |
| └───────────────────────────┘       |                                            |
|  （虚线框 = 码层级，未成单）          |  （实线框 = 订单层级，成单）                |
+-----------------------------------------------------------------------------------+
```

**菜单路径：**

```
菜单路径：进销存 > 下游返货券订单出库
权限可见性：不涉及（原型阶段无权限控制）
```

**字段规格表：**

| 字段名 | 含义 | 类型 | 取值来源 | 校验规则 | 示例 | 备注 |
|--------|------|------|---------|---------|------|------|
| 经销商待从下游返货 | 码层级待返货瓶数 | 数字 | 码核销记录接口（原型暂写死） | 非负整数 | 1250 | 单位「瓶」；不在订单列表 |
| 待出库（未发放乐享券） | 待出库订单瓶数合计 | 数字 | 品项汇总（skuList.pendingBottles 求和） | 非负整数 | 5542 | 单位「瓶」；点击打开待出库商品统计弹窗 |
| 已出库（已发放乐享券） | 已出库订单瓶数合计 | 数字 | 品项汇总（skuList.shippedBottles 求和） | 非负整数 | 1062 | 单位「瓶」 |

**静态文案：**

- **显式文案**：
  - 左段标题：「未生成返货订单」
  - 右段标题：「返货券订单」
  - 待返货卡说明：「消费者已核销，经销商尚未从下游取码生成返货订单」
  - 待出库卡说明：「按品项汇总待出库数量」
  - 已出库卡说明：「已自动发放乐享券」
- **气泡文案**：
  - `经销商待从下游返货 ⓘ` → `消费者扫码核销后、经销商尚未取码成单的返货券总数。尚未生成返货券订单，不在下方订单列表中。`
  - `待出库（未发放乐享券）ⓘ` → `经销商已取码生成返货券订单、但尚未确认出库的返货券总数。确认出库后将自动发放乐享券。`
  - `已出库（已发放乐享券）ⓘ` → `已确认出库、并自动发放乐享券的返货券总数。`

**交互点规格：**

| 交互点 | 后端处理逻辑 | 状态机/流转 | 触发校验规则 | 边界与异常 | 动效说明 |
|--------|-------------|-----------|-----------|---------|---------|
| 待出库卡片点击 | 无后端逻辑，仅打开弹窗 | 不涉及 | 不涉及 | 无数据时弹窗展示空态 | 无 |
| 各卡片 ⓘ 图标悬停 | 无后端逻辑，展示 tooltip | 不涉及 | 不涉及 | 不涉及 | 悬停渐显 |

### 5.1.2 业务流 / 信息流

```
页面加载
  → 读取码核销记录接口（待返货，原型暂写死 1250）
  → 读取品项汇总数据（skuList）
      → 计算待出库 = Σ pendingBottles
      → 计算已出库 = Σ shippedBottles
  → 渲染两段分栏：左段待返货（虚线框）/ 右段待出库 + 已出库（实线框）
```

### 5.1.3 正向案例

**案例 1（正向 — 查看两段层级指标）**

经销商出库人员张伟进入「下游返货券订单出库」页面，顶部统计区左侧展示「未生成返货订单」段，含「经销商待从下游返货 1,250 瓶」；右侧展示「返货券订单」段，含「待出库 5,542 瓶」与「已出库 1,062 瓶」。张伟通过左段虚线框、右段实线框区分「码层级」与「订单层级」。

**案例 2（正向 — 点击待出库卡看品项汇总）**

张伟点击右侧「待出库（未发放乐享券）」卡片，弹出「待出库商品统计」弹窗，按品项展示各商品待出库瓶数。

### 5.1.4 边界场景处理规则

| 场景 | 触发条件 | 系统行为 | 用户提示 |
|------|---------|---------|---------|
| 待返货数字为 0 | 无待返货码核销记录 | 左段展示 0 瓶 | 无特殊提示 |
| 品项汇总为空 | skuList 为空数组 | 待出库 / 已出库展示 0 瓶 | 无特殊提示 |

### 5.1.5 逆向流程

本子模块为只读统计展示，当前无逆向流程场景。

---

## 5.2 订单列表

### 5.2.1 页面原型

```
+--------------------------------------------------------------------------------------------------------------+
| 待出库   已出库                                          [批量确认出库 (N)]  [导出]                          |
+--------------------------------------------------------------------------------------------------------------+
| 单号: [____]  商户名称: [____]  下单时间: [____~____]  签收类型: [▼]  [查询] [↺]                          |
+--------------------------------------------------------------------------------------------------------------+
| 可处理及确认待出库的返货券商品单据                                          显示 N 条记录                        |
+----------+--------+--------+----------+--------+--------+--------+----+------+------+-----+------+------+------+
| ☐ | 商户名称 | 商户地址 | 老板手机号 | 含返货券数量 | 数量合计(瓶) | 数量合计(箱) | 核销费用(元) | 签收状态 | 下单人 | 下单时间 | ... | 操作 |
+----------+--------+--------+----------+--------+--------+--------+----+------+------+-----+------+------+------+
| ☐ | 坝区... | ...    | 136...   | 30     | 30.0   | 2.00   | 99.90 | 已签收 | 李李  | 08-08  | ... | 查看详情 确认出库 |
+--------------------------------------------------------------------------------------------------------------+
| 共 N 条记录，第 1 / 1 页                                                                                        |
+--------------------------------------------------------------------------------------------------------------+
```

**菜单路径：**

```
菜单路径：进销存 > 下游返货券订单出库（默认进入）
权限可见性：不涉及
```

**字段规格表（表格列）：**

| 字段名 | 含义 | 类型 | 取值来源 | 校验规则 | 示例 | 备注 |
|--------|------|------|---------|---------|------|------|
| 商户名称 | 订单所属下游商户 | 文本 | 订单数据 | 无 | 坝区喜品优选便利店 | — |
| 商户地址 | 商户经营地址 | 文本 | 订单数据 | 无 | 重庆市沙坪坝区… | 超长截断，悬停显示完整 |
| 老板手机号 | 商户老板联系电话 | 文本 | 订单数据 | 11 位 | 13629762784 | 等宽字体展示 |
| 含返货券数量 | 本单返货券数量 | 数字 | 订单数据 | 非负整数 | 30 | — |
| 数量合计(瓶) | 商品总瓶数 | 数字 | 订单数据 | 非负 | 30.0 | 高亮蓝色 |
| 数量合计(箱) | 商品总箱数 | 数字 | 订单数据 | 非负 | 2.00 | 两位小数 |
| 核销费用金额(元) | 核销费用 | 数字 | 订单数据 | 非负 | 99.90 | 两位小数 |
| 签收状态 | 物流签收进度 | 下拉枚举 | 订单数据 | 枚举值 | 已签收 | 绿色标签 |
| 下单人 | 下单操作人 | 文本 | 订单数据 | 无 | 李李 | — |
| 下单时间 | 下单时间戳 | 日期时间 | 订单数据 | 无 | 2026-08-08 13:12:26 | 等宽字体 |
| 司机手机号 | 配送司机电话 | 文本 | 订单数据 | 无 | 13982933080 | — |
| 司机姓名 | 配送司机 | 文本 | 订单数据 | 无 | 吕安建 | — |
| 签收人 | 签收操作人 | 文本 | 订单数据 | 无 | 李李 | — |
| 签收时间 | 签收时间戳 | 日期时间 | 订单数据 | 无 | 2026-08-08 13:12:26 | — |
| 出库人 | 出库操作人 | 文本 | 订单数据 | 无 | 邓福昌 | 未出库为空 |
| 出库时间 | 出库时间戳 | 日期时间 | 订单数据 | 无 | 2026-08-09 17:46:29 | 未出库为空 |

**查询条件：**

| 条件名 | 交互方式 | 来源 | 默认值 | 分页/排序规则 |
|--------|---------|------|--------|------------|
| 单号 | 手动输入 | 手动输入 | 无 | 模糊匹配 |
| 商户名称 | 手动输入 | 手动输入 | 无 | 模糊匹配 |
| 下单时间 | 日期范围选择 | 日期选择器 | 无 | 起止区间 |
| 签收类型 | 下拉选择 | 枚举 | 全类型 | 精确匹配 |
| 状态 tab | 点击切换 | 枚举（待出库/已出库） | 待出库 | 精确匹配 |

**静态文案：**

- **显式文案**：
  - 待出库 tab 提示：「可处理及确认待出库的返货券商品单据」
  - 已出库 tab 提示：「可查看已完成出库的返货券商品单据」
  - 空态：「暂无匹配的出库单据数据」

**交互点规格：**

| 交互点 | 后端处理逻辑 | 状态机/流转 | 触发校验规则 | 边界与异常 | 动效说明 |
|--------|-------------|-----------|-----------|---------|---------|
| 确认出库（单笔） | 更新订单 status → 已出库；记录出库人、出库时间；更新品项汇总 pending/shipped | 待出库 → 已出库 | 仅待出库订单可操作 | 重复点击幂等 | Toast 提示 |
| 批量确认出库 | 遍历选中订单逐一执行单笔出库逻辑 | 待出库 → 已出库 | 需至少勾选 1 条 | 部分失败逐条反馈 | Toast 提示 |
| 查看详情 | 打开出库单详情弹窗 | 不涉及 | 不涉及 | 不涉及 | 弹窗 |
| 导出 | 将当前筛选结果生成 CSV 下载 | 不涉及 | 不涉及 | 空结果导出空表头 | 浏览器下载 |

### 5.2.2 业务流 / 信息流

```
经销商确认出库
  → 系统校验订单状态 = 待出库
    → 满足 → 更新订单 status = 已出库 → 记录出库人/出库时间
              → 更新品项汇总（shipped += N, pending -= N）→ 自动发放乐享券 → 返回成功提示
    → 不满足 → 忽略（幂等），不重复出库
```

### 5.2.3 正向案例

**案例 1（正向 — 单笔确认出库）**

出库人员李明在「待出库」tab 找到订单 CK20260808001，点击「确认出库」，订单状态流转为「已出库」，出库人记录为「邓福昌」，出库时间为当前时间，页面弹出「单据 CK20260808001 出库成功！」提示，顶部「待出库」瓶数相应减少、「已出库」瓶数增加。

**案例 2（正向 — 批量确认出库）**

李明勾选 3 条待出库订单，点击「批量确认出库 (3)」，3 条订单全部流转为「已出库」，页面弹出「已批量完成 3 笔单据出库处理！」提示。

**案例 3（边界场景 — 已出库订单不可再出库）**

李明在「已出库」tab 查看订单，操作列仅显示「查看详情」，不再出现「确认出库」按钮，避免重复出库。

### 5.2.4 边界场景处理规则

| 场景 | 触发条件 | 系统行为 | 用户提示 |
|------|---------|---------|---------|
| 筛选无结果 | 查询条件无匹配 | 表格展示空态 | 「暂无匹配的出库单据数据」 |
| 待出库 tab 无数据 | 无待出库订单 | 表格空态，批量按钮隐藏 | 「暂无匹配的出库单据数据」 |
| 未勾选即批量操作 | selectedIds 为空 | 批量按钮不展示 | 无 |
| 导出空结果 | 当前筛选结果为空 | 下载仅含表头的 CSV | 无 |

### 5.2.5 逆向流程

| 逆向场景 | 触发条件 | 处理路径 | 回滚/补偿机制 |
|---------|---------|---------|-------------|
| 误操作出库需撤销 | 经销商误点确认出库 | 本期不支持在线撤销，走线下工单处理 | 待后续迭代补充「撤销出库」功能 |

---

## 5.3 待出库商品统计弹窗

### 5.3.1 页面原型

```
+--------------------------------------------+
| 📄 待出库商品统计                       [×] |
+--------------------------------------------+
| [全部] [元气森林气泡水] [外星人电解质水] ...  |
+--------------------------------------------+
| 🔵 冰茶 低糖 白桃茉莉 600ml    [600ml*15瓶/箱]|
|    品类: 冰茶                      待出库     |
|                                    60 瓶     |
+--------------------------------------------+
| 共计 N 个品项 | 待出库总量: 5,542 瓶         |
|                    [导出汇总数据] [确认]     |
+--------------------------------------------+
```

**菜单路径：**

```
菜单路径：进销存 > 下游返货券订单出库 > 点击「待出库」卡片
权限可见性：不涉及
```

**字段规格表：**

| 字段名 | 含义 | 类型 | 取值来源 | 校验规则 | 示例 | 备注 |
|--------|------|------|---------|---------|------|------|
| 品项名称 | 商品名称 | 文本 | 品项汇总数据 | 无 | 冰茶 低糖 白桃茉莉 600ml | — |
| 规格 | 商品规格 | 文本 | 品项汇总数据 | 无 | 600ml*15瓶/箱 | 标签展示 |
| 品类 | 商品分类 | 文本 | 品项汇总数据 | 无 | 冰茶 | — |
| 待出库 | 该品项待出库瓶数 | 数字 | 品项汇总数据 | 非负整数 | 60 | 单位「瓶」，蓝色高亮 |

**静态文案：**

- **显式文案**：
  - 弹窗标题：「待出库商品统计」
  - 空态：「所选分类下暂无待出库品项数据」

**交互点规格：**

| 交互点 | 后端处理逻辑 | 状态机/流转 | 触发校验规则 | 边界与异常 | 动效说明 |
|--------|-------------|-----------|-----------|---------|---------|
| 分类标签切换 | 按分类过滤品项列表 | 不涉及 | 不涉及 | 无匹配展示空态 | 无 |
| 导出汇总数据 | 将当前分类品项生成 CSV 下载 | 不涉及 | 不涉及 | 空结果导出空表头 | 浏览器下载 |
| 确认 / 关闭 | 关闭弹窗 | 不涉及 | 不涉及 | 不涉及 | 无 |

### 5.3.2 业务流 / 信息流

```
点击「待出库」卡片
  → 打开弹窗，默认「全部」分类
  → 用户点击分类标签 → 过滤品项列表 → 重算待出库总量
  → 点击导出 → 生成 CSV 下载
```

### 5.3.3 正向案例

**案例 1（正向 — 按分类查看待出库汇总）**

出库人员王芳打开「待出库商品统计」弹窗，默认展示全部品项；点击「冰茶」分类标签，列表仅显示冰茶类商品及对应待出库瓶数。

**案例 2（正向 — 导出汇总数据）**

王芳点击「导出汇总数据」，下载「返货券按品项待出库汇总_日期.csv」文件，包含品项名称、品项、规格、待出库（瓶）四列。

### 5.3.4 边界场景处理规则

| 场景 | 触发条件 | 系统行为 | 用户提示 |
|------|---------|---------|---------|
| 分类下无品项 | 所选分类无数据 | 展示空态 | 「所选分类下暂无待出库品项数据」 |

### 5.3.5 逆向流程

本子模块为只读统计，当前无逆向流程场景。

---

## 5.4 订单详情弹窗

### 5.4.1 页面原型

```
+-----------------------------------------------------+
| 📦 出库单详情  [CK20260808001] [待出库]          [×] |
+-----------------------------------------------------+
| 🏢 商户基础信息                                       |
|    商户名称: 坝区喜品优选便利店  老板手机: 157...      |
|    商户地址: 重庆市沙坪坝区...                         |
+-----------------------------------------------------+
| 🚚 配送/司机信息        | ✓ 签收与费用                 |
|   司机姓名: 吕安建      |   签收状态: 已签收           |
|   司机手机: 139...      |   签收人员: 李李             |
|   出库人员: 邓福昌      |   签收时间: 08-08 13:12     |
|   出库时间: 未出库      |   核销费用: ¥99.90          |
+-----------------------------------------------------+
| 📄 出库商品品项明细 (N项)   含返货券:30 | 总瓶数:30瓶  |
|  品项名称 | 分类 | 瓶数 | 箱数 | 单价(元) | 小计(元)  |
+-----------------------------------------------------+
| 下单人：李李 (2026-08-08 13:12:26)                    |
|                              [确认完成出库] [关闭]    |
+-----------------------------------------------------+
```

**菜单路径：**

```
菜单路径：进销存 > 下游返货券订单出库 > 订单列表「查看详情」
权限可见性：不涉及
```

**字段规格表：**

| 字段名 | 含义 | 类型 | 取值来源 | 校验规则 | 示例 | 备注 |
|--------|------|------|---------|---------|------|------|
| 商户名称 | 订单商户 | 文本 | 订单数据 | 无 | 坝区喜品优选便利店 | — |
| 老板手机 | 商户老板电话 | 文本 | 订单数据 | 无 | 15723236870 | — |
| 商户地址 | 商户地址 | 文本 | 订单数据 | 无 | 重庆市… | — |
| 司机姓名 / 手机 | 配送司机信息 | 文本 | 订单数据 | 无 | 吕安建 | — |
| 出库人员 / 时间 | 出库信息 | 文本/时间 | 订单数据 | 无 | 邓福昌 | 未出库显示「未出库」 |
| 签收状态 | 签收进度 | 枚举 | 订单数据 | 无 | 已签收 | — |
| 签收人员 / 时间 | 签收信息 | 文本/时间 | 订单数据 | 无 | 李李 | — |
| 核销费用 | 核销费用金额 | 数字 | 订单数据 | 非负 | 99.90 | 单位「元」 |
| 品项明细 | 商品行项目 | 表格 | 订单 items | 无 | — | 含品项/分类/瓶数/箱数/单价/小计 |

**交互点规格：**

| 交互点 | 后端处理逻辑 | 状态机/流转 | 触发校验规则 | 边界与异常 | 动效说明 |
|--------|-------------|-----------|-----------|---------|---------|
| 确认完成出库 | 与列表「确认出库」一致，更新状态并关闭弹窗 | 待出库 → 已出库 | 仅待出库订单显示该按钮 | 已出库订单不显示 | Toast 提示 |
| 关闭 | 关闭弹窗 | 不涉及 | 不涉及 | 不涉及 | 无 |

### 5.4.2 业务流 / 信息流

```
点击「查看详情」
  → 打开弹窗，展示商户/配送/签收/品项明细
  → 若状态 = 待出库，展示「确认完成出库」按钮
      → 点击 → 更新订单状态 → 关闭弹窗 → 列表同步刷新
```

### 5.4.3 正向案例

**案例 1（正向 — 查看订单详情）**

出库人员陈刚点击订单「查看详情」，弹窗展示商户信息、配送信息、签收与费用、品项明细，底部显示下单人及下单时间。

**案例 2（正向 — 从详情页确认出库）**

陈刚在「待出库」订单详情弹窗点击「确认完成出库」，订单状态流转为「已出库」，弹窗关闭，列表同步更新。

### 5.4.4 边界场景处理规则

| 场景 | 触发条件 | 系统行为 | 用户提示 |
|------|---------|---------|---------|
| 已出库订单查看详情 | 状态 = 已出库 | 不展示「确认完成出库」按钮，仅可关闭 | 无 |
| 未出库订单字段为空 | outboundTime/outboundUser 为空 | 展示「未出库」或「-」 | 无 |

### 5.4.5 逆向流程

本子模块当前无逆向流程场景。

---

# 六、迭代计划

（待人工维护）

---

# 七、运营计划

## 7.1 权限类

本期不涉及（原型阶段无权限控制，后续接入统一权限体系时补充角色矩阵）。

## 7.2 埋点类

本期不涉及（原型阶段未接入埋点，正式版需补充出库操作、导出等关键事件埋点）。

## 7.3 数仓类

本期不涉及（待确定出库数据回流数仓口径后补充）。

---

# 八、沟通记录

## 8.1 TBD 待解析汇总表

| 编号 | 描述 | 预计解析时机 | 状态 |
|------|------|-----------|------|
| todo-01 | 「经销商待从下游返货」数字目前写死 1250，需接入真实码核销记录接口 | 后端接口就绪后 | 未解析 |
| todo-02 | 「待出库/已出库」瓶数（品项汇总口径）与订单列表订单笔数口径未对齐，需统一口径 | 数据源确定后 | 未解析 |
| todo-03 | 订单表签收状态（已签收/待签收/运输中/已拒收）与出库状态的关系待梳理 | 需求澄清后 | 未解析 |
| todo-04 | 出库误操作是否需支持「撤销出库」 | 迭代规划时 | 未解析 |

## 8.2 外部依赖跟踪表

| 编号 | 依赖来源 | 供给内容 | 用途 | 对接方式 | 当前状态 |
|------|---------|---------|------|---------|---------|
| dep-01 | 码核销记录接口 | 消费者扫码核销的返货券码记录 | 计算「经销商待从下游返货」数量 | API | 待提供 |

---

# 九、参考资料

- 原型代码：`C:\Users\YQSL\返货券订单出库`（src/ 目录）
- 迭代记录：`./ITERATION.md`
- README：`./README.md`

---

<!-- PRD 基线区域 · 临时 · 正式成稿后删除 -->

## PRD 基线（临时 · new 模式）

### ① prd-prep 关联

| 字段 | 内容 |
|------|------|
| prd-prep 文件 | 无（本模块为原型迭代项目，基于现有原型代码 + 需求讨论直接落地，未执行 prd-prep） |
| prd-prep 完成日期 | 无 |
| prd-prep 版本状态 | 未执行 |
| 主要 TBD 项 | 见 §8.1 四条 todo |

### ② 书写进度看板

| 章节 | 状态 | 负责 Agent | 备注 |
|------|------|-----------|------|
| 一、文档概要 | 已完成 | Claude | — |
| 二、背景及目标 | 已完成 | Claude | — |
| 三、竞品分析 | 待人工 | — | 占位 |
| 四、主逻辑 | 已完成 | Claude | — |
| 五、方案详情 | 已完成 | Claude | 4 个子模块 |
| 六、迭代计划 | 待人工 | — | 占位 |
| 七、运营计划 | 已完成 | Claude | 本期不涉及 |
| 八、沟通记录 | 已完成 | Claude | 4 条 TBD + 1 条依赖 |
| 九、参考资料 | 已完成 | Claude | — |

### ③ changelog 编号区间

| 项目 | 内容 |
|------|------|
| 本次落地 changelog 编号 | 20260817-0001（v1.0 初始落地） |
| changelog 文件 | `change_log/PRD_change_log.md` |
